# Build

Build project targets.

## Usage

- `/build` - build all (default target)
- `/build <target>` - build specific target

## Instructions

Run: `cmake --build build --target $ARGUMENTS -j`

If `$ARGUMENTS` empty, omit `--target` flag.
